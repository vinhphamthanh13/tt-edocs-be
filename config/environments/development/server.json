{
  "host": "${process.env.APP_HOST || '127.0.0.1'}",
  "port": "${process.env.APP_PORT || 1337}",
  "proxy": {
    "enabled": false
  },
  "autoReload": {
    "enabled": true
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
