{
  "host": "${process.env.APP_HOST || 'localhost'}",
  "port": "${process.env.APP_PORT || 1337}",
  "proxy": {
    "enabled": false
  },
  "autoReload": {
    "enabled": true
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
